<h3 class="link-to-back"><%= link_to 'Voltar', job_opportunity_path(@job_opportunity) %></h3>

<div class="form-div-submit">
	<h2 class="form-title-submit" >Agendando Entrevista</h2>
	<p>Vaga: <%= @job_opportunity.title %></p>
	<p>Candidato: <%= @candidate.full_name %> - <%= @candidate.profession %></p>

	<%= render "form" %>
</div>
