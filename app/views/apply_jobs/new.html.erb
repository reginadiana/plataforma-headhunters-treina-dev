<h3 class="link-to-back"><%= link_to 'Voltar', job_opportunity_path(@job_opportunity) %></h3>

<div class="form-div-submit">
	<h2 class="form-title-submit">Você esta se candidatando para a vaga: <%= @job_opportunity.title %></h2>

	<%= render "form" %>
</div>
