<div class="field-seach">

	<p class="inst-seach">Pesquise por candidatos por suas competências</p>

	<%= form_with url: search_candidates_path, method: :get, local: true do |f| %>
		<%= f.label :q, 'Busca' %> 
		<%= f.text_field :q%>
		<button class="button-busca" type="submit">Pesquisar</button>
	<% end %>
</div>
